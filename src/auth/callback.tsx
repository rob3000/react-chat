import React, { Component } from 'react';

class Callback extends Component {
  render() {
    return (
      <div style={{
        background: '#f6f4fc',
        height: '100vh',
        textAlign: 'center',
      }}>
        Loading....
      </div>
    );
  }
}

export default Callback;
